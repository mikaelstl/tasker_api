enum BaseActions {
  CREATE = 'CREATE',
  DEL = 'DEL',
  EDIT = 'EDIT',
  SEEK = 'SEEK',
}

enum EnhancedActions {
  // MANAGE = `MANAGE`,
  
  PROMOTE = `PROMOTE`,
  DEMOTE = `DEMOTE`,
}

export {
  BaseActions,
  EnhancedActions
}