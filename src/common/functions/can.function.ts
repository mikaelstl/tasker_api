import { Actions } from "src/common/enums/Actions.enum";
import { Resources } from "src/common/enums/Resources.enum";

export const can = (
  action: Actions,
  resource: Resources,
  restriction: (object: any) => {}
) => {
  
}